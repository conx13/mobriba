import 'package:flutter/material.dart';

class OtsiElement extends StatelessWidget {
  const OtsiElement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Otsi elementi'),
      ),
    );
  }
}
